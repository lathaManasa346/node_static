<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Assignment 1</title>
</head>

<body>

    <h1>Books by My Author</h1>
            
               <a href="/books">View All Books</a>
                <a href="/order">Order a Book</a>

                <br>
                <br>

    <!-- form data for user to select book and number of copies on click of purchase button -->
  <form id="booksData" method="POST" action="/order">
  Errors: {{errors}}
    <select id="selectBook" name="isbn">
        <option value="0">Select a Book</option>
        {{#each products}}
        <option value="{{this.isbn}}">{{this.title}}</option>
        {{/each}}
    </select>
     {{!-- {{#if errors.isbn}}
        <p style="color: red;">{{errors.isbn}}</p>
        {{/if}} --}}
    <label for="numberOfCopies">Copies:</label>
    <input type="text" id="numberOfCopies" name="copies">
        {{!-- {{#if errors.copies}}
        <p style="color: red;">{{errors.copies}}</p>
        {{/if}} --}}
    <br><br>
    <button type="submit">Place Order</button>
</form>

<br><br>
    <i>copyright Suryakanth Gunnam</i>
</body>

{{!-- <form id="booksData" method="POST" action="/order">
        <!-- Book selection dropdown -->
        <label for="selectBook">Select a Book:</label>
        <select id="selectBook" name="product">
            <option value="0">Select a Book</option>
            {{#each products}}
            <option value="{{this.isbn}}" {{#if (eq ../formData.product this.isbn)}}selected{{/if}}>{{this.title}}</option>
            {{/each}}
        </select>
        <!-- Error message for book selection -->
        {{#if errors.product}}
        <p style="color: red;">{{errors.product}}</p>
        {{/if}}

        <!-- Number of copies input -->
        <label for="numberOfCopies">Number of Copies:</label>
        <input type="text" id="numberOfCopies" name="copies" value="{{formData.copies}}">
        <!-- Error message for number of copies -->
        {{#if errors.copies}}
        <p style="color: red;">{{errors.copies}}</p>
        {{/if}}

        <br><br>
        <button type="submit">Purchase</button>
    </form> --}}
    
</html>